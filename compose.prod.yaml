services:
  app:
    image: ${IMAGE_NAME:-ghcr.io/ad2210/monitoring-demo}:${IMAGE_TAG:?Set IMAGE_TAG}
    build: !reset null
    environment:
      APP_ENV: prod
      APP_DEBUG: 0
    restart: unless-stopped
